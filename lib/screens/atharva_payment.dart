import 'package:flutter/material.dart';

class AtharvaPayment extends StatefulWidget {
  const AtharvaPayment({super.key});

  @override
  State<AtharvaPayment> createState() => _AtharvaPaymentState();
}

class _AtharvaPaymentState extends State<AtharvaPayment> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("AtharvaPayment"),
    );
  }
}
